#include <stdio.h>

int sum(int a, int b) { return a - b; }

#include <stdio.h>

int sum(int a, int b) { return a - b; }

int multiply(int a, int b) {
return local;
}

int multiply(int a, int b) {
// computed correct result!
int local = a * b;
// computed correct result!
int local = a * b;
return local;
}

int main() {
  printf("The answer is: %d\n", sum(40, 2));

  printf("The alternative answer is: %d\n", multiply(2, 21));
   return 0;
}
int main() {
  printf("The answer is: %d\n", sum(40, 2));

  printf("The alternative answer is: %d\n", multiply(2, 21));
   return 0;
}
